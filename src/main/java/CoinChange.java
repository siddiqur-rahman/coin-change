import java.util.Map;
import java.util.HashMap;

public class CoinChange {

    public Map<Integer, Integer> make(int amount) {
        Map<Integer, Integer> changes = new HashMap<>();

        changes.put(1, 1);

        return changes;
    }
}
